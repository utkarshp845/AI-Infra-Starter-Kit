# Terraform

Terraform is intentionally not part of the Day 1 quickstart.

This directory exists to mark where cloud infrastructure examples can be added later, after the local and Kubernetes learning paths are clear.

Good future examples:

- Container registry.
- Managed Kubernetes cluster.
- Managed log storage.
- Secrets manager.
- Budget alerts.

Keep Terraform examples small and tied to a concrete lesson.

